@model DataLayer.Entities.Account

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="content-header">
    <h1>
        Accounts
        <small>Edit</small>
    </h1>
</section>

<!-- Main content -->
<section class="content">
    <div class="row">
        <!-- left column -->
        <div class="col-lg-12">
            <!-- general form elements -->
            <div class="box box-danger">
                <div class="box-header with-border">
                    <h3 class="box-title">Edit Account</h3>
                </div>
                <!-- /.box-header -->
                <!-- form start -->

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <form role="form">

                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                        <div class="box-body">

                            <div class="form-group">
                                <label for="Name">ID</label>
                                <input type="text" class="form-control" name="Id" id="Id" value="@Model.Id" readonly>
                                @Html.ValidationMessageFor(model => model.Id, "", new { @class = "text-danger" })
                            </div>

                            <div class="form-group">
                                <label for="Name">Customer ID</label>
                                <input type="text" class="form-control" name="CustomerId" id="CustomerId" value="@Model.CustomerId" readonly>
                                @Html.ValidationMessageFor(model => model.CustomerId, "", new { @class = "text-danger" })
                            </div>

                            <div class="form-group">
                                <label for="Name">Account Type ID</label>
                                <input type="text" class="form-control" name="AccountTypeId" id="AccountTypeId" value="@Model.AccountTypeId" readonly>
                                @Html.ValidationMessageFor(model => model.AccountTypeId, "", new { @class = "text-danger" })
                            </div>

                            <div class="form-group">
                                <label for="Name">Credit</label>
                                <input type="text" class="form-control" name="Credit" id="Credit" value="@Model.Credit">
                                @Html.ValidationMessageFor(model => model.Credit, "", new { @class = "text-danger" })
                            </div>

                        </div>
                        <!-- /.box-body -->

                        <div class="box-footer">
                            <button type="submit" value="Save" class="btn btn-default">Save</button>
                            @Html.ActionLink("Volver", "Index", "Account", null, new { @class = "btn btn-default" })
                        </div>

                    </form>
                }
            </div>
            <!-- /.box -->
        </div>
    </div>
    <!-- /.row -->
</section>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
